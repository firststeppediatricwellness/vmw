# MongoDB Connection String (Get this from MongoDB Atlas)
# Format: mongodb+srv://<username>:<password>@cluster.mongodb.net/vmwf_db?retryWrites=true&w=majority
MONGODB_URI=

# Secret key for JWT Authentication
JWT_SECRET=your_super_secret_key_change_me

# Port to run the server on (Render sets this automatically)
PORT=3000
